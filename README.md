# nmfuzz
CTF Enumeration Automation Tool. A bash script automates the entire nmap scan, tcp (SYN scan) and udp. Detects http/https servicies and launches both directory (gobuster) and subdomain (wfuzz) fuzzing.
